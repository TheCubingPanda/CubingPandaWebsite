import type { Camera, Scene, WebGLRenderer } from "three";
export declare function renderPooled(width: number, height: number, canvas: HTMLCanvasElement, scene: Scene, camera: Camera): Promise<void>;
export declare function newRenderer(): Promise<WebGLRenderer>;
