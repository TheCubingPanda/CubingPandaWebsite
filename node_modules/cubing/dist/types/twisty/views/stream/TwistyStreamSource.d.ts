import { ManagedCustomElement } from "../ManagedCustomElement";
interface StreamSource extends EventTarget {
    disconnect?: () => void;
}
export declare class TwistyStreamSource extends ManagedCustomElement {
    constructor();
    addSource(label: string, sourceClass: {
        connect: () => Promise<StreamSource>;
    }): HTMLButtonElement;
    addStreamSource(): void;
}
export {};
