import type { ManagedCustomElement } from "./ManagedCustomElement";
export declare class ClassListManager<SuffixType extends string> {
    #private;
    private elem;
    private prefix;
    private validSuffixes;
    constructor(elem: ManagedCustomElement, prefix: string, validSuffixes: SuffixType[]);
    clearValue(): void;
    setValue(suffix: SuffixType): boolean;
}
