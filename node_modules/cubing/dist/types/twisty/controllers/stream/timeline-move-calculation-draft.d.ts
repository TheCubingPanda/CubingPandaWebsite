import { Move } from "../../../alg";
import type { Duration, Timestamp } from "../AnimationTypes";
interface Event {
    timeStamp: Timestamp;
    move: Move;
}
export interface TimelineEntry {
    event: Event;
    start: Timestamp;
    end: Timestamp;
}
declare type Timeline = TimelineEntry[];
export declare function toAxes(events: Event[], diameterMs: Duration): TimelineEntry[][];
export declare function toTimeline(events: Event[], diameterMs?: number): Timeline;
export {};
