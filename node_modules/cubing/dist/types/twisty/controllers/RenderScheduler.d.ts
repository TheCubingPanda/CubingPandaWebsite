export declare class RenderScheduler {
    private callback;
    private animFrameID;
    private animFrame;
    constructor(callback: (timestamp: DOMHighResTimeStamp) => void);
    requestAnimFrame(): void;
    cancelAnimFrame(): void;
    private animFrameWrapper;
}
export interface Schedulable {
    scheduleRender(): void;
}
