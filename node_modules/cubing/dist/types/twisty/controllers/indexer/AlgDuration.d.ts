import { Alg, Grouping, LineComment, Commutator, Conjugate, Move, Newline, Pause, TraversalUp } from "../../../alg";
import type { Duration } from "../AnimationTypes";
export declare function constantDurationForAmount(_amount: number): Duration;
export declare function defaultDurationForAmount(amount: number): Duration;
export declare function ExperimentalScaledDefaultDurationForAmount(scale: number, amount: number): Duration;
export declare class AlgDuration extends TraversalUp<Duration> {
    durationForAmount: (amount: number) => Duration;
    constructor(durationForAmount?: (amount: number) => Duration);
    traverseAlg(alg: Alg): Duration;
    traverseGrouping(grouping: Grouping): Duration;
    traverseMove(move: Move): Duration;
    traverseCommutator(commutator: Commutator): Duration;
    traverseConjugate(conjugate: Conjugate): Duration;
    traversePause(_pause: Pause): Duration;
    traverseNewline(_newline: Newline): Duration;
    traverseLineComment(_comment: LineComment): Duration;
}
