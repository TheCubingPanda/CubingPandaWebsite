export declare class PromiseFreshener<T> {
    #private;
    queue(p: Promise<T>): Promise<{
        fresh: false;
    } | {
        fresh: true;
        result: T;
    }>;
}
export declare class StaleDropper<T> {
    #private;
    queue(p: Promise<T>): Promise<T>;
}
