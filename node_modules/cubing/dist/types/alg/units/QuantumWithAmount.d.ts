import type { Repeatable } from "../common";
import { IterationDirection } from "../iteration";
import type { LeafUnit } from "./Unit";
export declare class QuantumWithAmount<Q extends Repeatable> {
    readonly quantum: Q;
    readonly amount: number;
    constructor(quantum: Q, amount?: number);
    suffix(): string;
    isIdentical(other: QuantumWithAmount<Q>): boolean;
    experimentalExpand(iterDir: IterationDirection, depth: number): Generator<LeafUnit>;
}
