import { Alg, QuantumMove } from "../../../alg";
import type { KPuzzle } from "../../../kpuzzle";
import type { KState } from "../../../kpuzzle/KState";
import type { SGSCachedData } from "./parseSGS";
export declare class TrembleSolver {
    private kpuzzle;
    private sgs;
    private searchMoves;
    constructor(kpuzzle: KPuzzle, sgs: SGSCachedData, trembleMoveNames?: string[]);
    solve(state: KState, stage1DepthLimit?: number, quantumMoveOrder?: (quantumMove: QuantumMove) => number): Promise<Alg>;
    private sgsPhaseSolve;
}
export declare function randomStateFromSGS(kpuzzle: KPuzzle, sgs: SGSCachedData): Promise<KState>;
