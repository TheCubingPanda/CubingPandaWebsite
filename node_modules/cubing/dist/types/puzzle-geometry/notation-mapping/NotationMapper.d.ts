import { Move } from "../../alg";
import type { KPuzzleDefinition } from "../../kpuzzle";
export interface NotationMapper {
    notationToInternal(move: Move): Move | null;
    notationToExternal(move: Move): Move | null;
}
export declare function remapKPuzzleDefinition(internalDefinition: KPuzzleDefinition, notationMapper: NotationMapper): KPuzzleDefinition;
