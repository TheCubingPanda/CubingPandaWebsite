import { Move } from "../../alg";
import type { NotationMapper } from "./NotationMapper";
export declare class NxNxNCubeMapper implements NotationMapper {
    slices: number;
    constructor(slices: number);
    notationToInternal(move: Move): Move;
    notationToExternal(move: Move): Move;
}
