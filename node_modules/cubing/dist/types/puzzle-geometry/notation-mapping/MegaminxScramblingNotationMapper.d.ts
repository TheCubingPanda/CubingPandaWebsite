import { Move } from "../../alg";
import type { NotationMapper } from "./NotationMapper";
export declare class MegaminxScramblingNotationMapper implements NotationMapper {
    private child;
    constructor(child: NotationMapper);
    notationToInternal(move: Move): Move | null;
    notationToExternal(move: Move): Move | null;
}
