import { Move } from "../../alg";
import type { FaceNameSwizzler } from "../FaceNameSwizzler";
import type { NotationMapper } from "./NotationMapper";
export declare class FaceRenamingMapper implements NotationMapper {
    internalNames: FaceNameSwizzler;
    externalNames: FaceNameSwizzler;
    constructor(internalNames: FaceNameSwizzler, externalNames: FaceNameSwizzler);
    convertString(grip: string, a: FaceNameSwizzler, b: FaceNameSwizzler): string;
    convert(move: Move, a: FaceNameSwizzler, b: FaceNameSwizzler): Move;
    notationToInternal(move: Move): Move;
    notationToExternal(move: Move): Move;
}
