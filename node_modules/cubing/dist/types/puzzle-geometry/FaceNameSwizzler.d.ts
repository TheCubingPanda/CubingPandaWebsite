export declare class FaceNameSwizzler {
    facenames: string[];
    prefixFree: boolean;
    gripnames: string[];
    constructor(facenames: string[], gripnames_arg?: string[]);
    setGripNames(names: string[]): void;
    splitByFaceNames(s: string): number[];
    joinByFaceIndices(list: number[]): string;
    spinmatch(userinput: string, longname: string): boolean;
    spinmatchv(userinput: string, longname: string): boolean;
    unswizzle(s: string): string;
}
