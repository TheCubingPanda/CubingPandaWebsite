import { Seed, Scramble, Scrambler } from './types';
export declare class Scrambow {
    type: string;
    length: number;
    seed: Seed;
    args: string[];
    constructor(type?: string, length?: number);
    init(): void;
    get(num?: number): Scramble[];
    setType(type?: string): this;
    private getType;
    setSeed(seed?: number): this;
    setLength(length?: number): this;
    setArgs(...args: string[]): this;
    registerCustomScrambler(name: string, scrambler: Scrambler, aliases?: string[]): void;
}
